Считываю строку getline()

пока есть символы: for(int i = 0; i <= pos; i++)
	если str[i] цифра
		int c = i
		пока str[c+1] цифра
			res *= 10;
			res += цифра;
			c++;
		push в Stack число res;
		i = c;
	если str[i] не цифра
		если "="
			напечатать pop из Stack
			break;
		иначе если "-"
			pop два числа
			взять разность
			push в Stack
		иначе если "*"
			pop два числа
			перемножить
			push результат
		иначе если "+"
			pop два числа
			сложить
			push результат
		иначе если не " "
			печать ОШИБКА